---
name: passing-tests
description: Use when CI tests pass on a PR to check merge readiness
disable-model-invocation: true
---

Tests passed on the PR. Check to see if the PR can be merged. If so, do not merge it---wait for the user to review. If anything is blocking merge, resolve it. $ARGUMENTS
